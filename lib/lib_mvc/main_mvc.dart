import 'package:flutter/material.dart';

class MainMvcDefault extends StatelessWidget {
  const MainMvcDefault({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
